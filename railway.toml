[build]
builder = "NIXPACKS"
watch = ["backend/**"]

[deploy]
startCommand = "cd backend && npx prisma generate && npm run start:prod"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
